@page
@model Song_Bibliothek.Pages.Songs.CreateModel
@{
}


<br>
<h2>New Song</h2>

@if (Model.errorMessage.Length > 0)
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>@Model.errorMessage</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> 
    </div>
}

<form method="POST">
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Title</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="title" value="@Model.songInfo.title">
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Artist</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="artist" value="@Model.songInfo.artist">
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Album</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="album" value="@Model.songInfo.album">
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Track</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="track" value="@Model.songInfo.track">
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Year</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="year" value="@Model.songInfo.year">
        </div>
    </div>
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Lyrics</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="lyrics" value="@Model.songInfo.lyrics">
        </div>
    </div>

    @if (Model.successMessage.Length > 0)
    {
        <div class="row mb-3">
            <div class="col-sm-9">
                <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <strong>@Model.successMessage</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            </div>
        </div>
    }

    <div class="row mb-3">
        <div class="offset-em-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class=" btn btn-outline-primary" href="/" role="button">Cancel</a>
        </div>
    </div>
</form>